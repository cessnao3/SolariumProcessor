extern serial_write(src) = 0xA000;
extern serial_writeln(src) = 0xA000;
extern get_serial_dev() = 0xA000;

const HELLO_WORLD = "Hello, world!";
const NEWLINE = "\n";

func main()
{
    auto local_var = NEWLINE;
    auto local_array[40];
    local_array[5] = 3;

    //if, else, while
    serial_writeln()

    return 0;
}
